# BB-8 / D-O 开发续接记录

更新时间：2026-07-12 19:30（Asia/Shanghai）

## 当前可继续的主工程

- Blender：`blender/output/BB8_1to1_screen_referenced.blend`
- 建模生成器：`blender/create_bb8.py`
- 导出器：`blender/export_bb8.py`
- 重开审计：`blender/audit_bb8.py`
- 网站：`app/page.tsx`
- 物理参数：`engineering/physics_inputs.json`
- 物理验证器：`tools/verify_physics.py`

## 本阶段证据

- Blender 后台重开审计成功：292 个对象、88 个内部机构对象；GUI 同一进程无损重开阶段 9.3 主工程，当前仅 1 个 Blender GUI 进程且窗口无未保存星号。
- 场景元数据与可见几何：球体 508 mm、头部 295 mm、不含天线高度 670 mm。
- 身体、头部和内部机构 STL 以及 GLB 已从当前主工程重新导出，不再沿用旧外观文件。
- 阶段 1 物理解析模型在当前假设下 PASS：8.57 N 推进力、每电机 0.286 N·m、连续扭矩裕量 2.10×、磁保持裕量 2.51×。
- 网站构建及 2 项渲染回归测试通过。

## 文件指纹

- 主 `.blend`：`c4dd9fbd9a6f931992797ecbfa96d5a3559686047a40362821e7706e4b236ff0`
- 阶段 1 检查点：`6abceb677bfdd6b6bf13d89373fd49696c4f7c623092c28a5e4edbe72d4c5560`
- 阶段 2 检查点：`3ab31459d9aff2f7e7e3c92db93c5a4ac870eb6c74bf05abbb9dc13deb366133`
- 内部机构 STL：`4f5b67a637651efc8a223c7a63739cd1a7ba028972a56c118726261b8b7b0975`

## 未完成且不得误称完成

1. 外观仍是屏幕参考重建，尚未达到逐面板电影道具测绘精度。
2. 物理 PASS 基于假设输入，不是实物认证；需要称重、摩擦/预紧、扭矩电流、磁脱离力实测。
3. 编码器/IMU闭环、双INA226电流适配、过流/堵转回放和ESP32-S3编译已完成，但实体接线、参数辨识、ALERT→EN硬件门、热平衡、壳体接触/疲劳仍未完成。
4. D-O 付费 CAD 尚未购买；公开网站已部署到 `https://zhangyinxina-ui.github.io/bb8-do-assembly-guide/`。

## 下一步

拿到真实目标板、电机、驱动器、电池、MPU6050、编码器和双INA226板后，先核对GPIO/I²C、分流器额定与ALERT→EN外部门，再完成架空轮CPR/方向/IMU/电流标定，用阶跃响应替换仿真参数，随后进入封壳围栏低速验证。

## 阶段 2：多体、结构与电热（2026-07-12 14:27）

- 新增 `tools/verify_multibody.py`、`docs/BB8_多体结构电热验证.md` 和 `engineering/turning_multibody_sweep.csv`。
- 1.0 m/s、0.6 rad/s 转弯及冲击合载荷下，磁头解析保持裕量 2.31×。
- 12/8 mm、300 mm 铝管桅杆解析静态屈服裕量 18.52×；尚未计入疲劳、孔位和模态降额。
- 电机集总热模型估算设计点 38.9 °C。
- 低电量峰值电流会把 4S 电池拉到 12.55 V；控制核心已加入 13.2–14.3 V 连续功率降额并重新通过 ESP32-S3 编译。
- 阶段参数与扫描快照保存在 `engineering/checkpoints/stage02_multibody/`。

## 阶段 3：参数化内部总成（2026-07-12 14:31）

- 内部机构从 9 个示意对象增加到 25 个稳定标记对象。
- 新增左右电机座、四只随动稳定轮及支撑臂、左右桅杆三角支撑、两条电池固定带、电子托盘和保险丝/接触器包络。
- 生成内部正视、侧视、俯视三视图及组合图 `blender/output/BB8_internal_three_view.png`。
- 新增 `engineering/internal_assembly_manifest.csv`，记录每个机构对象的包络尺寸和世界坐标。
- 阶段检查点：`blender/checkpoints/BB8_stage03_buildable_internal_assembly.blend`。

## 阶段 4：真实电机候选与接触几何（2026-07-12 14:42）

- 厂家数据筛选首选候选：Sha Yang Ye / Cytron `IG42E-24K`，12 V、248 rpm、0.98 N·m额定扭矩、5.5 A额定电流。
- 96 mm轮理论额定地速1.25 m/s；相对0.286 N·m连续需求的额定扭矩裕量3.43×。
- Pololu 4692最大效率点扭矩约0.186 N·m，未作为当前连续工况首选。
- 内部电机包络更新为42 mm直径、100 mm保守总长、8 × 20 mm轴；对象数增至27。
- 修复此前驱动轮悬空问题：驱动轮中心位于206 mm接触半径，四只稳定球中心位于236 mm接触半径，审计公差±1 mm。
- 固件新增12 V电机母线归一化；4S满电16.8 V时PWM上限71.4%，ESP32-S3重新编译通过。
- 阶段检查点：`blender/checkpoints/BB8_stage04_IG42_contact_geometry.blend`。

## 阶段 5：真实电机面板、轮毂与拆装空间（2026-07-12 14:58）

- 从Cytron商品页关联的Sha Yang Ye `IG-42GM` 图纸确认：最大外径45 mm、24:1齿箱39.2 mm、电机段66 mm、8 × 20 mm轴、4×M4×6深/PCD35。
- 原阶段4的100 mm电机包络和150 mm轮距会造成左右电机实体重叠，已改为125.2 mm总包络和310 mm轮距，中央尾部间隙约19.6 mm。
- 主框由整板改为两根380 × 18 × 12 mm纵梁和8 mm中央横梁，避开电机实体；新增两块4 mm电机面板、8枚M4、两只26 × 40 mm键毂和6枚M5轮端紧固件包络。
- 内部对象增至45件；Blender重开审计新增电机中心检修间隙、PCD35实例、55 mm工具空间、球壳内包络和壳面接触检查。
- 控制器及90°转弯验证同步到310 mm轮距：0.3 m/s、0.8 m半径时左右轮为0.2419/0.3581 m/s。
- 新增 `docs/BB8_阶段5_电机安装与拆装.md`；键毂3×M5节圆和360/609 g重量来源冲突仍等待采购样件复测。
- 阶段检查点：`blender/checkpoints/BB8_stage05_verified_IG42_mount.blend`。

## 阶段 6：可拆球壳与显式线束（2026-07-12 15:00）

- 外观增加赤道连续分缝，内部增加494 mm密封圈包络和8个45°等分锁扣；单锁扣夹紧力合同暂定80 N。
- 左右IG42E分别建立动力线和编码器线：共12段显式线束、2个Molex 3.96 mm动力连接器和2个JST-PH 2.0 mm编码器连接器。
- 动力线最低弯曲半径合同30 mm，编码器20 mm；底盘边缘要求耐磨套，开壳前必须断开4个维护连接器。
- Blender重开审计扩展到70个内部对象，并验证8个锁扣、线束/连接器数量、弯曲半径合同、驱动轮保守包络10 mm避让及所有网格保持在球壳内。
- 新增 `docs/BB8_阶段6_球壳与线束维护.md`，包括开壳顺序和20次开合/手转10圈/低压架空测试门槛。
- 阶段检查点：`blender/checkpoints/BB8_stage06_serviceable_shell_harness.blend`。

## 阶段 7：磁性头部随动机构（2026-07-12 15:05）

- 内车顶端和头部底端各增加6个Ø20 × 6 mm磁体包络，面对面设计气隙8 mm；磁体等级、镀层和封装仍未冻结。
- 头底加入3只Ø24 mm非染色滚轮，按球面法向布置并从外侧接触254 mm球壳，另设独立滚轮载体环。
- 头部0.65 kg、2.5g垂向与1.0g横向冲击的合成载荷为17.16 N；2×最低值34.33 N，工程装机拉力验收线冻结为40 N。
- 新增 `tools/verify_magnetic_coupling.py`、`engineering/magnetic_coupling.json` 和 `docs/BB8_磁性头部耦合验证.md`；明确目录拉力不能替代实际壳体/气隙测力。
- Blender重开审计扩展到88个内部对象，验证6+6阵列、8 mm磁隙、40 N验收合同和三只滚轮球面接触。
- 阶段检查点：`blender/checkpoints/BB8_stage07_magnetic_head_follower.blend`。

## 阶段 8：D-O 合法自组资源与本机运行（2026-07-12）

- Printed Droid 控制仓库固定在提交 `e90aacdbe26a62fd4f0229d5504a3f2f3c409055`，推荐入口为 v3.4.3；共5个 Arduino 草图，仓库内整机机械 CAD/STL 为0。
- v1.1、v2.1、v3.4 各自 README 含个人非商业许可；旧 Mega/Nano 目录许可不明确，不能把整个仓库笼统称为开源。
- Printed Droid 的装配 PDF、接线 PDF 和 60 mm 无轮调试支架已保存并核验 SHA-256；调试支架并非整机结构件且无明确再分发许可，未放入网站下载区。
- `tools/audit_do_resources.py` 通过；机器清单在 `engineering/do_resource_manifest.json`。
- v3.4.3 已在项目隔离的 Arduino Mega 2560 工具链编译通过：Flash 43,198 B（17%），RAM 1,516 B（18%）。
- 网站构建与2项渲染测试通过，并已在 Chrome 打开 `http://localhost:3001/`。
- Blender 5.1.2 已在本机打开唯一活动主工程 `blender/output/BB8_1to1_screen_referenced.blend`；历史阶段检查点尚未删除，因为用户新增“只保留一个 Blender 工程”涉及不可逆清理，等待明确确认。

## 阶段 9：电影参考外观拓扑与可执行网站（2026-07-12）

- 主工程仍只有一个当前编辑源：`blender/output/BB8_1to1_screen_referenced.blend`。本阶段覆盖保存且关闭 Blender 自动 `.blend1` 版本生成，没有新增检查点工程。
- 身体六个主面板改为半角 35° 的球面区域，方向固定为 P1前、P2右、P3底、P4顶、P5左、P6后；环型数量冻结为 R1×3、R2×2、R3×1。
- 新增八个角区拓扑 T1a、T1b、T2、T3、T4、T5、T6、T7，以及六个面板的蓝/红/黄灯位审计。
- 头部增加 PSI 状态灯和长短两类天线元数据；主尺寸保持身体 Ø508 mm、头部 Ø295 mm、无天线高度 673 mm。
- Blender 后台关闭重开审计通过：261 个对象，其中 88 个内部机构对象；主工程 SHA-256 为 `a15c2518e8fbc4efd92c2b1ed67fca85832a7771abf83f91b0787d8501786835`。
- 三视图、STL 和 GLB 已从新主工程重新导出。网站装配区由 5 个概念步骤扩展为 24 个可验收步骤，包含零件、工具、动作、安全提示和本机进度记忆。
- 外观依据与未完成边界见 `docs/BB8_阶段8_外观拓扑与尺寸依据.md`；第三方权利边界见根目录 `NOTICE.md`。
- 仍存在 7 个历史 `.blend` 检查点、1 个 Blender 自动备份和 1 个网站下载副本。清理这些文件属于不可逆删除，必须等待用户明确确认；在确认前网站的 Blender 下载副本保持旧版并应视为暂停发布。

### 阶段 9.1 校对修正

- 逐页放大校对 Builders Club V3.1 后，修正了此前错误的环型对应：P1=R3，P2/P3=R2，P4/P5/P6=R1。
- 将六块面板共用的六辐图案替换为四桥结构；P1/P2/P5/P6 为对角桥，P3/P4 为正交桥。
- 六块面板分别增加视觉描摹的灰色弧段、肋条与灯舱，并写入独立 `pattern_variant` 和近似精度元数据；精确尺寸仍不能冒充官方 CAD。
- 新后台重开审计通过：265 个对象、88 个内部对象；加入程序化白色/橙色磨损材质后的主工程 SHA-256 为 `4df861cc3d8ee11e7cc8723c7791e3bf5a1b6a41bcf54e56abbb6fbbfebdbf87`。
- 网站新增六章直达、当前步骤媒体、80–180% 缩放、步骤完成勾选与刷新恢复；未新增 3D 依赖。GLB 已有 198 节点、194 网格和5条动画，正式节点级爆炸查看器仍需用户明确允许新增 `three` 运行依赖。

### 阶段 9.2 D-O 采购门控与三视图一致性

- 新增 `engineering/do_self_build_bom.csv`：24 项零件/子系统按可采购、冲突暂缓、负载未知、需工程设计和本地已就绪分类；不把来源示例值冒充最终选型。
- 新增 `docs/DO_自组采购与调试路线.md`：把合法文件、供电、IMU、遥控、声音、舵机、电机、系留平衡、低速行驶和热耐久拆成 D01–D16 验收门。
- 明确暴露 Printed Droid 公开说明的驱动板冲突：仓库根 README 为一块 MDD10A，v3.4 README 为两块 MD10C；电机、减速比、四个舵机负载和峰值电流未冻结前暂缓采购。
- 发现合成尺寸三视图仍引用旧六辐渲染，新增 `tools/build_bb8_three_view_sheet.py` 从当前正/侧/后视图可重复重建；新图 SHA-256 为 `aaa8d6bace45ac64b161f77dfe2a9146e5cc42d634f1c78a2f54b7deef04cd17`。
- Blender 审计增加三视图新鲜度与网站副本字节一致性门；后台重开仍为 265 个对象、88 个内部对象、6 个主面板、8 个角区和 508/295/673 mm。
- D-O 资源审计、Mega v3.4.3 固件编译、6 项 BB-8 运动/物理校验、网站构建和2项渲染测试全部通过；lint 0错误、3个 `<img>` 性能警告。
- 当前 Blender GUI 仍是带 `*` 的未保存旧内存状态，不能保存覆盖新主工程，也不能未经确认丢弃。生产构建把网站 `.blend` 副本复制进 `dist`，因此当前除主工程外共有10个历史/备份/网站/构建 `.blend` 副本；不可逆删除以及新增 `three` 依赖继续等待用户明确确认。

### 阶段 9.3 头部轮廓、670 mm 高度与装配网站实机验收

- StarWars.com 官方总高 0.67 m 与社区摄影测量的身体 Ø508 mm、头部 Ø295 × 197 mm 结合；头部轮廓下缘设为 `z=219 mm`，与球体上缘形成35 mm轮廓交叠，无天线几何总高精确为670 mm。
- 头壳重建为20 mm下锥、31 mm最大直径围带和椭圆半穹顶；删除四块错误的大圆橙板，增加11块下围带橙色分段、两道深色条带、细橙上缘、5块顶部石墨板、侧后维护板和缩短天线。
- 后台重开审计通过：292个对象、88个内部对象、6个主面板、8个角区、R1/R2/R3=3/2/1；主工程 SHA-256 为 `c4dd9fbd9a6f931992797ecbfa96d5a3559686047a40362821e7706e4b236ff0`。
- 最新三视图尺寸图为2728×1180，SHA-256 `09f9e155b7489bea25c5da63023390c6e0219f924684368010bc677294bfeac4`；GLB SHA-256 `bbf5ae88e22e5c8e96b4c54cbd87a33e950e7ee7ae2f582d8b89c6aabd55586c`。
- 装配网站改为每阶段2–3张正/侧/后/内部视图，分离“当前位置”和“实际完成率”，增加原生大图对话框；Chrome实机验证切换、弹窗、A01完成率1/24和本机存储后已清空测试状态。
- `npm test` 构建和2项HTML回归测试通过；lint 0错误、4个原生`<img>`性能警告。全仓 `tsc --noEmit`仍因既有Cloudflare全局类型声明缺失失败，不是本轮页面类型错误的充分证明，后续需补正式类型环境。
- 运动学、解析物理、多体/结构/电热、90°差速转弯、电机候选、磁耦合、D-O资源审计和Mega 2560固件编译全部重新通过；低电量峰值扭矩仍必须降额，磁体实际拉脱力仍需台架测量。
- GUI旧内存态已无损另存为 `blender/checkpoints/BB8_stage08_unsaved_gui_20260712_183729.blend`（SHA-256 `feae71f106221d76f751a086764a18efb1d146b19a03292d65c5384a449a375f`），随后在同一PID 87812内打开最新主工程；当前窗口无星号且只有1个Blender GUI进程。
- 当前共有11个 `.blend` 文件：8个追加式阶段检查点、1个主工程、2个未在页面链接的旧网站/构建副本。任何删除仍属于不可逆清理，未执行。

## 阶段 10：编码器 / IMU 闭环运动与失效停车（2026-07-12 19:03）

- `firmware/controller_core.*` 新增左右编码器轮速PI、IMU偏航率修正、运动传感器新鲜度和IMU/编码器一致性门槛；锁存故障由5类增加到7类。
- `tests/closed_loop_sim_test.cpp` 直接复用生产C++控制核心，以200 Hz模拟电机惯性、滚阻死区、编码器量化、电池压降、热模型和双轮位姿。
- 确定性场景通过：0.30 m/s巡航段RMS误差0.00772 m/s，转角91.20°；0.20 m/s重新起步后注入IMU过期，当前周期PWM归零，0.8 s后速度0.00224 m/s。
- 完整遥测保存为 `engineering/closed_loop_telemetry.csv`，报告为 `docs/BB8_阶段10_闭环运动仿真.md`；网站提供两者下载。
- ESP32适配器现在启用`require_motion_feedback`，但在真实IMU/编码器适配落地前显式保持新鲜度为false，因此硬件EN维持关闭，不能误把仿真PASS当作可落地运行许可。
- 本阶段未修改Blender几何；主工程仍为阶段9.3文件，后台重开审计和唯一GUI进程约束将在完整回归中复核。

## 阶段 11：ESP32 编码器与 MPU6050 适配（2026-07-12 19:18）

- 新增 `firmware/sensor_math.*` 与 `tests/sensor_math_test.cpp`，独立验证正反正交解码、无效跳变拒绝、轮速换算、MPU6050陀螺比例、倾角、重力范数和编码器750 ms响应超时。
- ESP32-S3适配器新增GPIO 10–13双编码器四边沿CHANGE中断、原子计数快照、96 mm轮径速度换算和α=0.20低通。
- 编码器每轮计数默认0且不持久化；必须每次上电发送`E <CPR> <左方向> <右方向>`。有运动需求但无脉冲时锁存停机。
- 新增GPIO 17/18 MPU6050 400 kHz I²C适配、0x68/0x69探测、±4g/±500°/s、42 Hz DLPF、400样本静止零偏和30 ms新鲜度门槛。
- Arduino-ESP32 3.3.10 / Generic ESP32-S3编译通过：程序367791 bytes（28%），RAM24636 bytes（7%）。
- 报告保存为 `docs/BB8_阶段11_ESP32传感器适配.md`，机器合同为 `engineering/sensor_adapter_contract.json`；实体板、传感器、编码器、电机和电流保护仍未接入验证。
- 完整回归通过：网站渲染、Pages 54文件审计、C++控制/传感器单测、闭环仿真、全部工程计算和D-O资源审计均为PASS；Blender主工程后台重开仍为292个对象/88个内部对象，GUI仅保留1个进程且窗口无未保存星号。

## 阶段 12：双 INA226 电流保护与堵转回放（2026-07-12 19:30）

- 新增 `firmware/power_safety.*`，只接受每次上电显式输入的瞬时电流、堵转电流和持续时间；未配置时拒动。
- ESP32-S3适配新增0x40/0x41双INA226、2 mΩ Kelvin分流合同、1 mA LSB、`0x0A00`校准、20 ms新鲜度和200 ms寄存器回读。
- 双ALERT开漏并联到GPIO14，并明确要求外部门同时直接拉低驱动EN；仅读GPIO不冒充硬件限流。
- 控制核心新增电流传感器过期、硬件ALERT、测量过流和持续堵转4类锁存，总计11类。
- 5 ms确定性回放覆盖同帧过流、0.300 s堵转、传感器过期和硬件告警；记录为 `engineering/power_safety_replay.csv`。
- Generic ESP32-S3实际编译通过：程序369971 bytes（28%），RAM24676 bytes（7%）。实体分流、限流、堵转、保险丝与热试验仍为NOT_RUN。

## 阶段 13：电流保护硬件进入内部模型（2026-07-12 19:54）

- 电子托盘新增双34 × 22 × 1.6 mm INA226包络、双12 × 5 × 3 mm/2 mΩ四线分流器、6个3 mm高M2.5支柱和28 × 14 × 1.6 mm独立ALERT→EN门板。
- 新增11条保护链线束：高侧输入2、监测后输出2、Kelvin采样4、开漏ALERT 2、失效低电平EN 1；随内车继承-4°到+4°俯仰动画。
- 内部对象由88增至110，总对象由292增至314；508/295/670 mm、6个主面板、8个角区、R1/R2/R3=3/2/1和原机械审计均保持PASS。
- 主工程、阶段13 v3检查点、内部四视图、内部STL、动画GLB和110件CSV装配清单已重建；v1/v2修正前检查点不删除并在检查点README中明确标为被取代。
- `physical_test_status`仍强制为`NOT_RUN`；真实四线焊点、ALERT断MCU、分流器脉冲温升、架空堵转、保险丝配合和封壳热平衡尚未执行。

## 阶段 14：质量/质心闭环与英文网站（2026-07-12 20:20）

- 新增17组最小/名义/最大质量账本、质心与三轴惯量计算，并穷举131072个最小/最大质量角点组合；名义8.463 kg、质心z=-56.2 mm，最不利角点z=-27.7 mm。
- 取消未经证明的110 mm偏置作为设计事实；若在z=-220 mm附近强求该偏置还需约4.14 kg，无法保持当前驱动余量。
- Blender新增120 × 70 × 24 mm、名义1.50 kg密封钢配重盒、中央挂板、双束带与4枚M5防松紧固件，以及名义/旧假设两个工程标记。
- 后台关闭重开审计通过：324个总对象、120个内部对象，其中118个制造对象进入STL/CSV，2个工程标记被导出脚本明确排除；主工程SHA-256为`291c80fa1ef2399459f78693f42f3d8b9cd7eeaf4a2b5f633d9cc59ffa0b94f9`。
- 按8.463 kg名义质量重跑解析物理：0.70 m/s²设计点每台电机需求0.299 N·m，0.6 N·m连续额定余量2.01×；真实称重、质心、结构、热与带电台架仍为`NOT_RUN`。
- 新增独立英文站入口`/en/`，完整包含24步装配、BB-8阶段14证据、D-O资源/许可边界、下载和中英文切换；与中文站共用同一批模型与校验文件。
